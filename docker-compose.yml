version: '3.8'

services:
  dashboard:
    build:
      context: ./dashboard
      target: dev
    ports:
      - "3000:3000"
    volumes:
      - ./dashboard:/app
      - /app/node_modules  # Named volume to avoid overwriting
    environment:
      - NODE_ENV=development